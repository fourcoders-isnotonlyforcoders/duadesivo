import { MainContainer } from "./styles";
import { FirstSection } from "../FirstSection";
export const Main: React.FC = () => {
  return (
    <MainContainer>
      <FirstSection />
    </MainContainer>
  );
};
